# Cross-plan execution order — LOCKED active set (2026-05-30)

> Regenerated 2026-05-30 after an adversarial review of all 10 candidate changes (+ synthesis).
> **9 active changes, 21 ordered steps.** (The active set: wf05b, entity-rag, registry-catalog-consistency,
> widget-role-access, core-data-model-hardening, unified-conversation-flow, smart-report-screen,
> wf04-tool-coverage-completion, onboarding-shell-shared-view.) `cf19` (broken — rework), `wf10`, `cf04`
> are BACKLOGGED (see bottom). `core-data` is kept whole (your call) but its run-critical items run first;
> the rest are opportunistic.
>
> **Progress: steps 1-9 landed as of commit `c7f08be`** (RAG resolver, RCC Catalog, WRA role/scope guard
> + per-widget sweep, core-data GeneratedResult + ScopedViewerWidget base + citations). The base is shipped
> but ORPHANED — wiring is discharged at step 19 (see step 7/19 notes). Steps 10-21 remain.
>
> **Workflows retired (2026-05-30).** Every step is now **SEQUENTIAL** — single-threaded, no
> parallel subagent fan-out. The steps that were formerly tagged WORKFLOW are still single units of
> work; do them sequentially and gate per coherent sub-unit (one widget / one tool / one view) — see
> **"Multi-file steps"** below. The `mode` column is uniformly `SEQ` and kept only so row numbers and
> cross-references stay stable.

## Per-task adversarial review gate
Discipline **§10** is in force: **a task is not done until a hostile review of its output passes —**
against the plan AND the real code, not the seam. The default gate (falsify claims vs code · no-op /
dead-plumbing check · `validate --strict` · open the test file · cross-plan collision check · build +
guards) lives in `discipline.md` §10 + `principles.md`. **Bespoke per-step gates are generated lazily
when a step is picked up** (not pre-generated — the set is still settling and the prior 40 are stale).
Multi-file steps gate PER coherent sub-unit (one widget / one tool / one view), done in sequence.

## Order at a glance
| # | mode | change | phase | blocked by |
|--|--|--|--|--|
| 1 | SEQ | wf05b-word-level-geometry | Pure resolver + fixture test (tasks 8-10), DEFER live -118-map fetch wiring | — |
| 2 | SEQ | entity-rag-scope-roundtrip | Phase 1 — failing round-trip test + correct stale comments (app.ts:802-803, ch | — |
| 3 | SEQ | registry-catalog-consistency | Phase 1 — shared Catalog<T> + assertUniqueIds (with module-path sourceOf upgra | — |
| 4 | SEQ | widget-role-access | Phase 1 — shared WidgetRole/WidgetScope enums + policy + flip widget-contract  | 3 |
| 5 | SEQ | widget-role-access | Phase 2a — access-matrix coverage test | 4 |
| 6 | SEQ | core-data-model-hardening | Item 2 — shared GeneratedResult shape (Result half of Template+Scope+Results) | 4 |
| 7 | SEQ | core-data-model-hardening | Items 5/7 — ScopedViewerWidget base + registry + show_* registration (PdfViewe | 3, 4, 6 |
| 8 | SEQ | widget-role-access | Phase 2b — per-widget prop sweep (mode→role + scope, README, sibling test) for | 5, 7 |
| 9 | SEQ | core-data-model-hardening | Item 6 — promote ChatMessage.citations + rewire InteractView off poll + delete | 1, 7 |
| 10 | SEQ | unified-conversation-flow | Phase 1 — extract useConversation (pure dedup, point both forks at one hook) | 9 |
| 11 | SEQ | unified-conversation-flow | Phase 2 — one <ConversationFlow> + ChatExperience + chatExperienceRegistry; de | 3, 4, 10 |
| 12 | SEQ | unified-conversation-flow | Phase 3 — remove mount-persistence routing hack (ChatColumnInner ~:172) + regr | 11 |
| 13 | SEQ | registry-catalog-consistency | Phase 2 — align toolRegistry + ScenarioRegistry; resolve toolRegistry orphan ( | 3, 4 |
| 14 | SEQ | registry-catalog-consistency | Phase 3 — rename EntityRegistry → EntitySessionStore (atomic tsc-guarded pass) | — |
| 15 | SEQ | smart-report-screen | Phase 0-2 — failing test + frame/nav wiring (f4a, remove reportActive, f4/f4a  | 7, 8, 6 |
| 16 | SEQ | smart-report-screen | Phases 3-4 — SmartReportRender (f4/S3) + SmartReportBuilder (f4a/S3a) + genera | 15 |
| 17 | SEQ | smart-report-screen | Phases 5-6 — pin affordance + per-control *.tools.ts + show_ verb allowlist ad | 11, 13, 16 |
| 18 | SEQ | wf04-tool-coverage-completion | §3 GateChatPanel stray Button wiring + §6 binding-guard widening (DropdownMenu | 11, 13 |
| 19 | SEQ | onboarding-shell-shared-view | Phase 1-2 — <ScopedCanvas> selector + OnboardingShell canvas swap (delete canv | 7, 11, 15, 14 |
| 20 | SEQ | onboarding-shell-shared-view | Phase 3-4 — retire per-frame views (Understand/Extract/Interact/Integrate/Sche | 19 |
| 21 | SEQ | registry-catalog-consistency | Phase 4 — declare chatExperienceRegistry as Catalog<ChatExperienceEntry> imple | 3, 11 |

## Step reasons
- **1. [SEQ] wf05b-word-level-geometry — Pure resolver + fixture test (tasks 8-10), DEFER live -118-map fetch wiring** — RAG-correctness, independent of app-arch refactor; must land before core-data removes the InteractView amount-due hack. Ship pure resolver now; processId/map-fetch infra has no precedent (backlog).
- **2. [SEQ] entity-rag-scope-roundtrip — Phase 1 — failing round-trip test + correct stale comments (app.ts:802-803, chatSessionEntities.ts, data-model.md:100) + explicit logged fallback** — Live RAG-correctness bug (scalar scope cols written by nothing); runs early + independent. No real steady-mode producer exists, so scope is the documented-gap test + comment fixes, NOT a new producer.
- **3. [SEQ] registry-catalog-consistency — Phase 1 — shared Catalog<T> + assertUniqueIds (with module-path sourceOf upgrade)** — Pure foundation primitive; unified Ph2 chatExperienceRegistry and core-data registry items build on it. One file, one contract.
- **4. [SEQ] widget-role-access — Phase 1 — shared WidgetRole/WidgetScope enums + policy + flip widget-contract guard mode→role + required scope** — Guard flip is the gating contract for unified ChatColumn, core-data ScopedViewerWidget, smart-report widgets, onboarding-shell. Builds role axis onto RCC-refactored registry, not legacy. Verify ContentScope shape stable first.
- **5. [SEQ] widget-role-access — Phase 2a — access-matrix coverage test** — Single shared gate test enumerating every widget dir vs matrix; must be green before the per-widget sweep so omissions fail fast.
- **6. [SEQ] core-data-model-hardening — Item 2 — shared GeneratedResult shape (Result half of Template+Scope+Results)** — Foundation contract unifying ExtractedFieldValue/RenderedSection; smart-report depends on it. Aligns with role/scope world from WRA Ph1.
- **7. [SEQ] core-data-model-hardening — Items 5/7 — ScopedViewerWidget base + registry + show_* registration (PdfViewer documentId→scope per WRA Phase 2b)** — The base class IS the contract; coupled type-driven work. Builds on RCC Catalog + WRA role/scope rule. Precedes smart-report + onboarding-shell. **STATUS (adversarial review 2026-05-30): base + `scopeKey`/`useScopeAdapter` + registry helper LANDED (TDD), but they are currently ORPHANED — zero production importers, no registry singleton, and PdfViewer does NOT build on the base (it took `scope` directly in step 8 and rolls its own reload).** The load-bearing wiring — stand up the production registry singleton (keep the shipped `Catalog`, add `kind: CanvasKind` + a `tools` SET to the descriptor; `CanvasKind` lists only BUILT kinds), construction-time totality over declared kinds + a runtime mount-every-declared-kind test + an ESLint import ban — is NOT a step-7 deliverable; it lands at **step 19** (ScopedCanvas is the only mount path that makes the registry load-bearing). Tracked by core-data's "OUTSTANDING — the base is ORPHANED; wire it up" ticket. (Direction-2 — a built-but-unregistered widget — has no airtight static catch; see that ticket for the honest scope.)
- **8. [SEQ] widget-role-access — Phase 2b — per-widget prop sweep (mode→role + scope, README, sibling test) for the 9 non-ChatColumn widgets + _template** — Per-widget work done sequentially (gate per widget) against fixed contract + matrix. ChatColumn carved out (coupled to unified). PdfViewer scope swap coordinated with core-data item 5. **DONE + reviewed 2026-05-30.** Scope: this step only does the *prop* swap (`documentId`→`scope`) — it did NOT wire PdfViewer onto the ScopedViewerWidget base or stand up the registry singleton (that is step 19 / the core-data orphan ticket, NOT here). Adversarial review found + fixed: optional `scope` regression, a missing anonymous-only availability test, matrix inaccuracy, 3 READMEs missing `## Events`, and 3 READMEs still documenting `role?`/`scope?` as optional after the required-prop migration. Guard hardened: rule-3 README header-presence (the spec scenario that was unimplemented — `existsSync`-only) + a README-must-not-document-optional-props check, both proven red-on-injection. Residual gap surfaced: widget-contract rule 5 (dependency direction) is still unenforced and `ChatColumn` violates it → ticketed on unified (step 11/Deferred).
- **9. [SEQ] core-data-model-hardening — Item 6 — promote ChatMessage.citations + rewire InteractView off poll + delete UTILITY demo hack** — Client-side citation field + InteractView rewire; deletes the amount-due hack now that wf05b supplies a real tight bbox. Coordinate ChatColumn write-citations with unified rewrite.
- **10. [SEQ] unified-conversation-flow — Phase 1 — extract useConversation (pure dedup, point both forks at one hook)** — Coupled single-file refactor defining ConversationApi; safe stopping point. After core-data citation/LiveTurn foundations landed.
- **11. [SEQ] unified-conversation-flow — Phase 2 — one <ConversationFlow> + ChatExperience + chatExperienceRegistry; delete forks + flow mode; ChatColumn rewrite (role prop)** — Imports RCC Catalog<T>; ChatColumn rewrite consumes WRA role + drops flow mode (guard already flipped). Tightly coupled multi-file contract.
- **12. [SEQ] unified-conversation-flow — Phase 3 — remove mount-persistence routing hack (ChatColumnInner ~:172) + regression test** — Meaningless before the fork is gone; persistence now structural via one always-mounted ConversationFlow.
- **13. [SEQ] registry-catalog-consistency — Phase 2 — align toolRegistry + ScenarioRegistry; resolve toolRegistry orphan (DOCUMENT decision only, defer delete)** — Orphan decision must reconcile with WRA SERVER_TOOL_CATALOG role axis + core-data. Document the delete-vs-wire decision; do not execute a half-done removal this run.
- **14. [SEQ] registry-catalog-consistency — Phase 3 — rename EntityRegistry → EntitySessionStore (atomic tsc-guarded pass)** — One transaction across ~3 real consumers (OnboardingSessionContext, test). Must precede/coordinate with onboarding-shell which rewrites the only runtime consumer.
- **15. [SEQ] smart-report-screen — Phase 0-2 — failing test + frame/nav wiring (f4a, remove reportActive, f4/f4a routing) + report data model on shared Template+ContentScope, consume ScopedViewerWidget base** — Consumes core-data base/GeneratedResult + WRA role/scope contract. Fixes f4 render-layer mis-route (OnboardingShell.tsx:407-409), not the intent.
- **16. [SEQ] smart-report-screen — Phases 3-4 — SmartReportRender (f4/S3) + SmartReportBuilder (f4a/S3a) + generalize PendingSchemaOverlay→PendingTemplateOverlay** — Two SmartReport widgets built sequentially against the shared base (gate per widget); the overlay generalization touches live Extract editing code (sequential within the builder unit).
- **17. [SEQ] smart-report-screen — Phases 5-6 — pin affordance + per-control *.tools.ts + show_ verb allowlist add + SERVER_TOOL_CATALOG mirror + render endpoint (Utility v1)** — Per-tool work (sequential, gate per tool) mirrored on server catalog. show_ allowlist add lands EXACTLY ONCE here. Coordinate ChatColumn pin path with unified rewrite + RCC/WRA catalog axes. Phase 7 (live multi-doc Solar) backlogged on WF-10.
- **18. [SEQ] wf04-tool-coverage-completion — §3 GateChatPanel stray Button wiring + §6 binding-guard widening (DropdownMenu/GxPill/GxSectionHeader prop-type refactor) + §7 inert-trio docs** — After unified stabilizes ChatColumn/ThinkingStream and RCC catalog consistency lands. §1/§2/§4 new tools backlogged pending verb-allowlist taxonomy + glob-home decision + onboarding-shell direction.
- **19. [SEQ] onboarding-shell-shared-view — Phase 1-2 — <ScopedCanvas> selector + OnboardingShell canvas swap (delete canvasContent useMemo, mount ConversationFlow experience)** — TERMINAL consumer. AppShell mount already exists (proposal §Why stale); real work is canvas selector swap. Needs ScopedCanvas over core-data base + makeOnboardingExperience + SmartReport widget. **This is where the ScopedViewerWidget registry becomes LOAD-BEARING and the step-7 orphan is discharged:** `ScopedCanvas` resolves `step.kind` → registry → component as the SOLE mount path, so land here (a) the production registry singleton built from the shipped `createScopedViewerWidgetRegistry([...])` `Catalog` (PdfViewer now; SmartReport[+builder] appended after step 16), descriptor extended with `kind: CanvasKind` + a `tools` SET; (b) **Direction 1** — `switch (step.kind)` over `CanvasKind` (built kinds only) with a `never` default + construction-time "one descriptor per declared kind" — guarantees every *declared* kind resolves to a widget (NOT widget-set completeness); (c) a runtime test mounting every *declared* `CanvasKind` (undeclared kinds → labelled placeholder, not crash); (d) an ESLint `no-restricted-imports` rule banning direct `viewer-widgets/*` imports outside the registry. **Direction 2** (a built-but-unregistered widget) has NO airtight static catch — mitigated by sole-mount-path + the import ban + `knip --production` + a best-effort matrix doc-coverage check (NOT the `show_*` heuristic; it would miss PdfViewer). Decide the Report builder/render shape (one `report` kind w/ mode vs separate `report-builder` kind) before wiring — see core-data ticket.
- **20. [SEQ] onboarding-shell-shared-view — Phase 3-4 — retire per-frame views (Understand/Extract/Interact/Integrate/Schema) + align SteadyShell to ScopedCanvas** — Per-view thin-wrapper/deletion sweep + SteadyShell's own canvas-fork removal. Grep guard must also cover SchemaView. Phase 4 is safest deferral if time-boxed.
- **21. [SEQ] registry-catalog-consistency — Phase 4 — declare chatExperienceRegistry as Catalog<ChatExperienceEntry> implementer + conformance test** — Only actionable after unified ships the registry. One declaration + one test. If unslotted, remains a tracked checkbox — do NOT pre-create the registry.

## Multi-file steps — do sequentially, gate PER sub-unit
> (Formerly "fan-out" steps. Workflows are retired — these are no longer parallelized. The unit
> breakdown below is kept because it's the right granularity to **gate** at: finish one widget / tool /
> view, pass its adversarial review, then the next.)
- **WRA per-widget prop sweep** (widget-role-access Phase 2b (step 8)) — units: One agent per widget: each migrates its own .tsx (mode→role, add scope), README (Locked affordances + Scope section), and sibling *.test.tsx against its own matrix row. ~9 widgets + _template. EXCLUDES ChatColumn (coupled to unified). PdfViewer heaviest: documentId→ContentScope across 5 JSX mount sites in 4 view files (SteadyShell, InteractView, UnderstandView ×2, ExtractView) + tools.test.
  - _why these units:_ No cross-widget contract once Phase 1 enum + Phase 2a coverage test are green; most edits mechanical/local.
- **SmartReport render + builder widgets** (smart-report Phases 3-4 (step 16)) — units: Unit A: SmartReportRender (f4/S3) + CiteChip reuse + edit affordance + ReportRenderView. Unit B: SmartReportBuilder (f4a/S3a) + Save gate — its dependency PendingSchemaOverlay→PendingTemplateOverlay generalization is sequential-within-unit (touches live Extract editing).
  - _why these units:_ Both widgets independent against the shared ScopedViewerWidget base + shared report types from Phase 2.
- **SmartReport tools + render endpoint** (smart-report Phases 5-6 (step 17)) — units: One unit per control: co-located *.tools.ts mirrored on SERVER_TOOL_CATALOG, each independently testable (tool == UI control mutation). Plus the render endpoint POST /api/widgets/smart-report/reports/render as its own unit. SHARED serialize points: the single show_ ALLOWED_VERBS add + the SERVER_TOOL_CATALOG file.
  - _why these units:_ Per-tool parity work fans out; only the verb-allowlist line and catalog file are contention points.
- **wf04 guard + docs cleanup** (wf04 §3/§6/§7 (step 18)) — units: Unit A: §3 GateChatPanel Button wiring (existing tools). Unit B: §6 binding-guard widening — guard widening + 3 component prop-type refactors sequential, then a per-site resolution sweep. Unit C: §7 per-file no-llm.md/widget-contract.md doc edits.
  - _why these units:_ Independent files; §6's guard-contract edit gates its own per-site sweep.
- **onboarding-shell view retirement** (onboarding-shell Phases 3-4 (step 20)) — units: One agent per retired view: Understand/Extract/Interact/Integrate/Schema → thin wrapper or deletion. Plus SteadyShell canvas-fork alignment as its own unit. Single shared grep guard at the end (must also cover SchemaView + decide IngestView.manifest.hero handling).
  - _why these units:_ Each view removed independently once ScopedCanvas mounts widgets directly.

## Cross-plan conflict map
- **[HIGH] app-architecture/spec.md edited by FIVE changes, not four** _(widget-role-access, registry-catalog-consistency, core-data-model-hardening, smart-report-screen, wf04-tool-coverage-completion)_
  - Brief named only WRA/RCC/SRS/CDM; wf04 ALSO adds requirements (tool-binding drift guard) — verified by two reviewers. Serialize all five spec merges by archive order. Recommended archive order: core-data → widget-role-access → registry-catalog-consistency → smart-report → wf04 (wf04 last, ADDED-only, prose references mode-era contract and must be reconciled to WRA's role pivot at merge). WRA carries the MODIFIED requirement headers (mode→role); the other four are ADDED-only with no header collision. CDM's CanvasIntent/ViewerStepKind deltas partly document already-shipped work.
- **[HIGH] cf19 is largely unimplementable as written — exclude from run, ship only the cleanup slice** _(cf19-ensure-bucket-group-helper, entity-rag-scope-roundtrip, core-data-model-hardening)_
  - Reviewer found: (1) wrong endpoint POST /v1/groups → should be singular /v1/group; (2) helper creates an EMPTY group and never calls group_addbucket, so the multi-bucket search returns nothing; (3) false idempotency-on-name claim (GroundX has no name dedup → leaks a group per cold start); (4) false data model — ChatSessionEntityRecord has scalar bucketId, NO bucketIds array to branch on; (5) stale {kind:...} scope shape (core-data migrated kind→type); (6) wrong client axis (customer X-API-Key, not Partner); (7) TODO marker at chatRouter.ts:911 not :852. No producer writes a multi-bucket signal even after entity-rag. RECONCILED: do NOT run cf19's wiring this set. Limit to deleting the stale TODO(CF-19) and correcting durable-spec wording; the multi-bucket pivot feature is backlogged until a real upstream producer exists.
- **[HIGH] entity-rag column-drop (§9) collides with cf19/CF-15 substrate** _(entity-rag-scope-roundtrip, cf19-ensure-bucket-group-helper, core-data-model-hardening)_
  - entity-rag Phase 2 §9 would drop any scope column lacking a producer (all four are perpetually NULL today). But CF-15 design intent + cf19 depend on bucketId/groupId/projectIdsJson/documentIdsJson as the steady-mode substrate. RECONCILED: entity-rag must NOT exercise the drop branch. Scope it to the failing-round-trip documentation test + stale-comment fixes + an explicit logged fallback. Route any genuine dead-column removal through core-data's round-trip closeout (item 17), which already carves these columns to entity-rag — keep the boundary so neither double-edits the chat_session_entities mapping.
- **[HIGH] core-data is a 20-item monolith far over WIP cap=3 — cut to foundations** _(core-data-model-hardening, smart-report-screen, onboarding-shell-shared-view)_
  - Reviewer: 20 numbered items + 5 drift guards + 1637-line chatRouter split + 8-context factory + 7-error ApiError base + deferred DB-drop. Only items 2 (GeneratedResult), 5/7 (ScopedViewerWidget base), 6 (ChatMessage.citations) are run-set-critical foundations for smart-report + onboarding-shell. RECONCILED: in-set = items 2/5/6 (+ already-mostly-done 4/9/10). Everything else (chatRouter split, ApiError base, entity/context factories, type-unification sweeps, workflow-audit batches, recurrence guardrails, DB drop) → backlog. Without the cut this single change blocks the locked run for weeks.
- **[MED] SERVER_TOOL_CATALOG (toolCatalog.ts) + toolCatalog.test.ts edited by four changes** _(widget-role-access, smart-report-screen, wf04-tool-coverage-completion, core-data-model-hardening)_
  - WRA adds availableIn:WidgetRole[] to every entry + chat-router role filter; smart-report mirrors report tools; wf04 adds tools; core-data item 18 upgrades the test to description-level. No automated app↔server parity guard exists today (memory's 'drift guard green' is a phantom — verified). RECONCILED order on the catalog: WRA establishes the role axis (step 4 enum, step 8/13 catalog), THEN smart-report's report tools (step 17) and wf04's any-new-tools declare availableIn against it. Add an explicit minimal parity assertion or backlog it deliberately — do not rely on a nonexistent guard. show_ verb-allowlist add lands once (smart-report step 17).
- **[MED] ChatColumn.tsx touched by unified, WRA, smart-report, core-data, wf04** _(unified-conversation-flow, widget-role-access, smart-report-screen, core-data-model-hardening, wf04-tool-coverage-completion)_
  - unified rewrites it (deletes forks, removes flow mode, restructures ThinkingStream/SuggestedActionChips); WRA adds role prop; smart-report adds pin-to-report path + show-report→f4 intent lives here; core-data item 6 writes citations on append; wf04 §7 documents it as inert. RECONCILED via sequence: WRA Ph1 guard flip (step 4) → core-data citation write (step 9) → unified rewrite is the single authoritative ChatColumn edit (step 11) → smart-report pin path (step 17) + wf04 §7 docs (step 18) land after the rewrite stabilizes. ChatColumn is the carve-out from WRA's Phase 2b per-widget sweep.
- **[MED] X-Ray type field-name drift (pageNumber vs number vs page) double-authored by wf05b + core-data** _(wf05b-word-level-geometry, core-data-model-hardening)_
  - wf05b citationGeometry.XrayDoc uses documentPages[].pageNumber; core-data flags a separate X-Ray type in groundxDocumentsEntity.ts using .number vs .page, and explicitly says 'couples to wf05b — coordinate, don't double-fix'. RECONCILED: wf05b lands first (step 1) and owns the citationGeometry word-map addition; core-data's promote-to-shared canonical X-Ray type must adopt wf05b's field naming, not invent a third. Single shared definition.
- **[MED] chat-routing/spec.md edited by cf19, entity-rag, wf05b** _(cf19-ensure-bucket-group-helper, entity-rag-scope-roundtrip, wf05b-word-level-geometry)_
  - All three add to specs/chat-routing/spec.md (different requirements: scope-resolution vs scope-write vs citation-geometry). Additive, low textual conflict. RECONCILED archive order: wf05b → entity-rag → cf19(cleanup-only). Serialize to avoid OpenSpec validate churn.
- **[MED] toolRegistry orphan decision shared by RCC, core-data, WRA, wf04** _(registry-catalog-consistency, core-data-model-hardening, widget-role-access, wf04-tool-coverage-completion)_
  - App toolRegistry singleton has ZERO production importers (verified — live LLM catalog is middleware SERVER_TOOL_CATALOG). RCC Phase 2 must decide delete-vs-wire, reconciled with WRA's server role axis and core-data's 'widgets+tools as real base+registry'. RECONCILED: RCC Phase 2 (step 13) DOCUMENTS the decision only; defer any actual delete to a coordinated follow-up to avoid a half-done removal across three plans. wf04's new tools (if run) must satisfy whatever consistency contract RCC lands.
- **[LOW] GateChatPanel vs GateChatPanel naming mismatch** _(onboarding-shell-shared-view, unified-conversation-flow)_
  - onboarding-shell proposal/design + ui-views spec reference GateChatPanel; no such component exists — the real chat-side gate is GateChatPanel.tsx and the slot mounts ChatColumn. RECONCILED: treat as GateChatPanel; if a rename is intended it belongs to unified's ChatColumn rewrite (step 11), not onboarding-shell. Update the ui-views spec text.
- **[LOW] wf04 verb-allowlist + glob-home blockers for new tools** _(wf04-tool-coverage-completion, registry-catalog-consistency)_
  - submit_/wizard_/close_ are NOT in check-tool-quality ALLOWED_VERBS (only dismiss_ passes); and OnboardingWizard (a view) + DialogTitle (a primitive) are outside the registry/quality globs (chat-widgets/ + viewer-widgets/ only), so their *.tools.ts are invisible to both runtime registry and guard. RECONCILED: §1/§2/§4 new tools are backlogged. The verb-taxonomy decision (same family as the show_* removal) and any glob widening should be a deliberate call — fold glob-widening into RCC if pursued.

## Backlogged (NOT in this run)
- **RUN COMPLETE 2026-05-31 — steps 10-24 shipped + archived** (unified-conversation-flow, registry-catalog-consistency, smart-report-screen, onboarding-shell-shared-view → `openspec/changes/archive/2026-05-31-*`).
- **BACKLOG REGROUPED 2026-05-31 → 7 new plans** (everything except the 3 excluded items below). Each turns its blockers into explicit unblock tasks; product/design dependencies are `INPUT NEEDED` tasks sequenced FIRST. The partially-shipped source changes stay ACTIVE (not archived — their deltas aren't fully shipped) with a header pointer to the new plan; archive each once its new plan lands:
  - **`2026-05-31-tool-system-completion`** ← widget-role-access Phase 3 (LLM-tool role gating) + wf04 §1/§2/§4. 3 INPUT gates (orphan-migration / parity-guard / glob-home shape) + verb-allowlist/glob-home unblocks. _(wf04 §3/§6/§7 dropped — stale.)_
  - **`2026-05-31-word-level-citation-geometry`** ← wf05b tail (live `-118-map` fetch → tight bbox). Investigate-first: processId discovery (escalates to INPUT if it needs a credential).
  - **`2026-05-31-steady-scope-producer`** ← entity-rag Phase 2 producer + §9 column drop. **INPUT #1: the steady/BYO scope-production model.** (cf19's multi-bucket producer also waits on this.)
  - **`2026-05-31-onboarding-experiences`** ← Workspace/Project experiences + nav-rail enable + SchemaView fallback retirement. **INPUT #1: the two experiences' UX/content.**
  - **`2026-05-31-smart-report-followups`** ← initial-render round-trip + manual live-verify. (Phase 7 live multi-doc stays gated on `wf10` below.)
  - **`2026-05-31-dependency-direction-guard`** ← rule-5 guard + ChatColumn→`@/views/` untangle.
  - **`2026-05-31-core-data-followups`** ← remaining core-data refactors (chatRouter split, ApiError base, factories, type-unification, gated DB drops).
- **`wf04-tool-coverage-completion`** — superseded by `tool-system-completion` (§1/§2/§4); §3/§6/§7 dropped as stale. Husk retained for history only.
- **`cf19-ensure-bucket-group-helper`** — broken as written (wrong endpoint, empty-group bug, false data model, `kind`→`type` scope, wrong client axis). Rework checklist in its proposal; reconcile with entity-rag when reworked.
- **`wf10-loan-solar-content-seed`** — blocked on real Loan/Solar source assets.
- **`cf04-page-usage-event-reader`** — parked until the BYO free-tier budget surface is prioritized.
- **Deferred slices of active changes:**
  - _cf19-ensure-bucket-group-helper_: cf19 multi-bucket→group helper (entire wiring) — Unimplementable as written (wrong endpoint, empty-group bug, false idempotency, false data model, stale scope shape, wro
  - _entity-rag-scope-roundtrip_: entity-rag Phase 2 §9 column-drop — The four scope columns have a designed future producer (cf19 + steady mode); dropping them now removes substrate cf19/CF
  - _entity-rag-scope-roundtrip_: Real steady-mode/BYO scope producer (entity→bucketId/documentId resolver) — No non-sample EntityKind, no BYO upload path, no server entity→scope resolver exists today. Defer to the unified/onboard
  - _core-data-model-hardening_: chatRouter.ts 1637-line split — Pure refactor, not a smart-report prerequisite; large single-file serial effort. Defer.
  - _core-data-model-hardening_: Shared ApiError base + 7-error refactor — Independent cleanup, no foundation dependency. Backlog.
  - _core-data-model-hardening_: Entity CRUD factory + 8-context factory + SdkActionResult union (items 11/15) — Large cross-cutting refactor unrelated to the 2/5/6 foundations the run needs. Backlog unless capacity.
  - _core-data-model-hardening_: Items 17/18/20 (round-trip closeout, wire-types module, illegal-states union) — Substantial, not blocking smart-report. Backlog the bulk.
  - _core-data-model-hardening_: Item 19 assertChatSessionOwnership 403-guard consolidation — Security-critical but independent; worth an early standalone spawn rather than riding the monolith. Flagging for a separ
  - _core-data-model-hardening_: Recurrence guardrails (a-e drift guards, _template scaffold, reconciliation matrix) — Author AFTER the bases they guard exist; defer to closeout of foundation work.
  - _core-data-model-hardening_: Type-unification + workflow-audit batches (items 12/13, DB row-mapper validation, envelope — Low-risk dedup sweeps; not run-set-critical. Opportunistic/backlog.
  - _core-data-model-hardening_: Deferred DB sweep — drop extraction_schemas — Deploy-gated; must soak one release after templates migration. Stays deferred.
  - _smart-report-screen_: smart-report Phase 7 — live multi-doc Solar render (search_groundx fan + WF-06b verify) — Hard-blocked on WF-10 real Solar source assets (out of scope this run). Phases 0-6 are fixture-backed and shippable. Def
  - _wf05b-word-level-geometry_: wf05b live -118-map.json fetch wiring (processId discovery + storage-URL build + map fetch — Genuinely new middleware plumbing with no precedent. Ship the pure resolver + fixture test now, keep exact tier dormant-
  - _wf04-tool-coverage-completion_: wf04 §1/§2/§4 new tools (submit_signup, wizard_*, close_dialog) — Blocked by missing verb-allowlist entries + glob-home for view/primitive tools; wizard/DialogTitle surfaces are actively
  - _widget-role-access_: App↔server tool-catalog parity guard — No such guard exists today (memory's 'drift guard green' is phantom). Authoring it is extra scope; either add a minimal 
  - _registry-catalog-consistency_: RCC Phase 4 chatExperienceRegistry conformance declaration — Only actionable after unified ships the registry. If RCC archives first, keep as a tracked checkbox / spawned ticket — d
  - _unified-conversation-flow_: unified deferred block (shared main view, nav-rail→experience wiring, makeWorkspace/Projec — Correctly excluded; owned by onboarding-shell-shared-view or future changes. Keep out of this run.
  - _onboarding-shell-shared-view_: onboarding-shell Phase 4 (align SteadyShell) — Lowest-risk deferral candidate if the set runs long — SteadyShell already works via its own doc-viewer branch + RT-01..0
  - _widget-role-access_: WRA server-side RBAC enforcement, per-action capability granularity, real future roles — Explicitly out of scope (client role is affordance-only, not a security boundary; YAGNI). Defer to a dedicated security 
  - _widget-role-access_: WRA GA real-reasoning token streaming — Already carved out as a separate future OpenSpec change; keep deferred.

_Synthesis summary: 21-step global order across 9 active changes. Spine: RAG-correctness pair first (wf05b pure resolver → entity-rag round-trip-test), then the two foundation primitives (RCC Catalog<T> → WRA guard flip mode→role+scope), then core-data foundations (GeneratedResult → ScopedViewerWidget base → ChatMessage.citations) interleaved with WRA's per-widget sweep (step 8), then unified-conversation-flow (useConversation extract → one ConversationFlow + ChatColumn rewrite → remove the mount-persistence hack), RCC alignment (toolRegistry/ScenarioRegistry decision → EntityRegistry rename), smart-report (frame/nav wiring → render+builder widgets → tools+render endpoint), and finally onboarding-shell `ScopedCanvas` + per-view retirement — **which is where the ScopedViewerWidget registry becomes load-bearing and the step-7 orphan is discharged** (production singleton over the shipped `Catalog`; construction-time totality over *built* `CanvasKind`s + mount-every-declared-kind test + ESLint import ban; Direction-2 has no airtight static catch) — closing with RCC's chatExperienceRegistry conformance declaration (step 21). **Workflows retired 2026-05-30 — all 21 steps run sequentially, gating per coherent sub-unit.**_