# Finding Register

| ID | Severity | Axis | Status | Evidence type | Source evidence | User-visible impact | Expected model | Handoff |
|---|---|---|---|---|---|---|---|---|
| SCF-001 | low | Source of truth / round-trip done | confirmed | source + issue/backlog + test | `middleware/src/services/structuredHandler.ts:118`; `docs/agents/cross-plan-execution-order.md:128`; final GitHub searches for `CF-04`, `page_usage_event`, page budget, and pages remaining found no existing issue; `middleware/src/services/structuredHandler.test.ts` passed | A user asking page-budget usage gets the budget ceiling plus a frank note that live usage count is not wired. | Either ship a `page_usage_event` reader with write/read/render coverage or track the deferred reader in GitHub backlog. | issue `#13` <https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/13> |
| SCF-002 | low | Source of truth / round-trip done | confirmed | source + issue/backlog + test | `middleware/src/services/entityScopeProducer.ts:57`; `middleware/src/services/entityScopeProducer.ts:95`; `docs/agents/cross-plan-execution-order.md:94`; `docs/agents/cross-plan-execution-order.md:126`; final GitHub searches for `CF-19`, multi-bucket, and group resolver found no existing issue; `middleware/src/services/entityScopeProducer.test.ts` passed | No immediate shipped multi-bucket producer is present, but source/docs refer to a future group helper/substrate without GitHub handoff. | Keep the dormant substrate honest: track the future multi-bucket/group resolver in GitHub backlog or explicitly retire the stale plan wording. | issue `#14` <https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/14> |
| SCF-003 | medium | Production reuse / wireframe fidelity | confirmed | source + issue/backlog | `app/src/views/Steady/SteadyShell/SteadyShell.tsx:147`; `app/src/views/Steady/SteadyShell/SteadyShell.tsx:158`; GitHub `#5` | `/c/:sessionId` steady mode has document-viewer parity when a doc step exists, but broader steady wireframe/widget parity remains unaccepted and previously measured as incomplete. | Treat steady mode as its own production surface: verify `/c/:sessionId`, `/workspaces`, and `/projects` with Chrome evidence and do not count generic/unavailable canvas states as widget parity. | existing-issue `#5` |
| SCF-004 | medium | Production reuse / source of truth | confirmed | source + issue/backlog | `app/src/appConfig.ts:136`; `app/src/appConfig.ts:167`; `app/src/contexts/OnboardingContext/OnboardingProvider.tsx:17`; `app/src/contexts/OnboardingContext/OnboardingProvider.tsx:63`; `app/src/views/Onboarding/OnboardingWizard.tsx:55`; final GitHub searches for `OnboardingWizard`, scaffold walkthrough, and starter Home page copy found no existing issue | A signed-in user whose metadata lacks `onboardingState: "complete"` can see a globally mounted wizard with generic scaffold copy such as "Replace the starter Home page..." instead of GroundX-specific product onboarding. | Either disable/retire the scaffold walkthrough, or make it product-real and aligned with the F-series/steady onboarding model and tool contracts. | issue `#15` <https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/15> |
| SCF-005 | low | Real data | no-action | source + tests | `app/src/components/viewer-widgets/Extract/Extract.tsx:223`; `app/src/components/viewer-widgets/Extract/Extract.tsx:256`; `app/src/components/viewer-widgets/Extract/Extract.tsx:262`; `app/src/views/Onboarding/SchemaView.test.tsx:307` | Extract can fall back to manifest schema/sample values when a live doc id is unresolved or live fetch fails; SchemaView is already live-only and tested. | Do not treat this fallback as a current shipped defect unless the code/docs require Extract to be live-only; keep future manifest stripping as a planned migration. | no-action in this review; transitional fallback |
| SCF-006 | medium | Template/scope/results | confirmed | source + issue/backlog + tests | `shared/src/index.ts:122`; `openspec/specs/data-tier/spec.md:176`; `app/src/views/Onboarding/OnboardingShell.tsx:463`; `app/src/widgets/reportFixtures.ts:159`; `middleware/src/services/reportRenderer.ts:143`; GitHub `#11`; `middleware/src/services/contentScopeVocabulary.test.ts` passed | SmartReport Utility report/template lookup can be restored and still miss seeded/searchable documents because the report path keys demo scope as `filter.project` while seeded docs/search/RBAC contract on `filter.projectId`. | Align SmartReport demo/render/template scope with shared `ContentScope.filter.projectId` or explicitly translate legacy report keys at the route boundary. | existing issue `#11`; subfinding comment <https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/11#issuecomment-4617034014> |
| SCF-007 | low | Source of truth / widget/tool contracts | confirmed | source + issue/backlog + tests | `app/src/tools/types.ts:117`; `app/src/tools/types.ts:127`; `app/src/tools/catalog-parity.test.ts`; `middleware/src/services/toolCatalog.ts`; final GitHub searches for `toolRegistry`, app-side registry, and `SERVER_TOOL_CATALOG` found no exact issue | No direct user impact today because the live LLM surface is middleware `SERVER_TOOL_CATALOG`, but a stale duplicate app registry/handler layer can drift and mislead future tool changes. | Delete the orphan app-side registry/handlers or deliberately wire them into the production tool path with parity coverage. | issue `#16` <https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/16> |
| SCF-008 | medium | Source of truth / Template/scope/results | confirmed | Chrome + source + issue/backlog + tests | Chrome DevTools `/projects` snapshot/eval showed `Project · bucket 28454 · filter {"project":"utility"} · 1 sample ready`; `app/src/views/Scoped/ScopedConversationShell.tsx:87`; `app/src/conversation/experiences/project/experience.test.tsx:13`; `openspec/specs/data-tier/spec.md:176`; `middleware/src/services/groundxSearch.compose.test.ts:15`; final GitHub search for scoped `filter.project` drift returned only adjacent issues | The project-scoped route and tests can build a scope that does not match the current seeded-document/search/RBAC vocabulary, so project chat/report/extract requests may miss scoped documents or drift from the data-tier contract. | Normalize scoped project routes/tests to `filter.projectId` or introduce a deliberate translation boundary with tests proving the compiled GroundX filter matches seeded docs. | issue `#17` <https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/17> |
