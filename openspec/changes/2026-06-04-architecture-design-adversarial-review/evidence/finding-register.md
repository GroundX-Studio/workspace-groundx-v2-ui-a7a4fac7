# Finding Register

| id | severity | status | axis | evidence | impact | expected model | handoff |
|---|---|---|---|---|---|---|---|
| ADR-001 | medium | confirmed | Planning hygiene; Canvas intent architecture | `openspec/specs/app-architecture/spec.md:250-256` says `registerAdapter` is retired and no widget uses it; `openspec/specs/app-architecture/spec.md:715-726` says it is retained and names live callers; code confirms live callers in `app/src/views/Onboarding/OnboardingWizard.tsx:33-36`, `app/src/components/primitives/DialogTitle/DialogTitle.tsx:33`, `app/src/components/viewer-widgets/SignUpWidget/SignUpWidget.tsx:186`, and `app/src/contexts/CanvasOrchestratorContext/CanvasOrchestratorContext.tsx:52-65`. | Future OpenSpec execution can choose the wrong invariant: either delete a live adapter plane or preserve it while another active requirement says it is forbidden. | Durable specs should have one current requirement for the intent execution model; if adapters are retained, the retired/no-widget paragraph should be superseded or removed. | [#18](https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/18) |
| ADR-002 | high | confirmed | Data contracts; Round-trip done; Smart Report scope vocabulary | Current durable specs require product project scope to use `filter.projectId` (`openspec/specs/data-tier/spec.md:176-190`, `openspec/specs/ui-views/spec.md:716-727`, `shared/src/index.ts:122-129`). `/projects` follows that contract and guards it (`app/src/views/Scoped/ScopedConversationShell.tsx:83-93`, `app/src/views/Scoped/projectScopeVocabulary.test.ts:13-20`). Smart Report/onboarding report still uses and tests `filter.project`: `app/src/views/Onboarding/OnboardingShell.tsx:467-482`, `app/src/widgets/reportFixtures.ts:152-184`, `middleware/src/services/reportRenderer.ts:83-153`, `middleware/src/services/reportRenderer.test.ts:44-48`, `app/src/widgets/reportFixtures.test.ts:8-20`, `app/src/components/viewer-widgets/SmartReportRender/SmartReportRender.test.tsx:147-153`. The live render request passes `input.scope` to `/api/widgets/smart-report/reports/render` (`app/src/api/smartReport.ts:142-149`), and middleware compiles the received filter as-is (`middleware/src/services/groundxSearch.ts:211-214`). | A signed-in or sample Smart Report render can route through a locally-consistent but globally-stale scope key, missing documents indexed by canonical `projectId` and weakening the RBAC/search contract tests that were meant to prevent `filter.project` drift. | Smart Report app fixtures, report template routing, report renderer doc index, render endpoint tests, widget tests, and static guards should migrate to `filter.projectId`; any non-product legacy `project` fixture should be explicitly isolated and excluded from product scope paths. | [#19](https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/19) |
| ADR-003 | high | confirmed | Security/privacy; Observability; Frontend analytics consent | The durable security spec requires a cold-load consent banner and no GA/Hotjar/PostHog load until consent (`openspec/specs/security-and-privacy/spec.md:12-31`). Current app bootstrap calls `initAnalytics(import.meta.env.VITE_POSTHOG_API_KEY, ...)` and `initGa(import.meta.env.VITE_GA_MEASUREMENT_ID)` before rendering (`app/src/main.tsx:9-24`). `initAnalytics` calls `posthog.init` as soon as the key exists (`app/src/lib/analytics.ts:40-49`), and `initGa` injects the `googletagmanager.com/gtag/js` script as soon as the measurement id exists (`app/src/lib/ga.ts:48-72`). Source search found no app consent gate/cookie implementation. | EU-touched or privacy-sensitive deployments that configure PostHog/GA can load third-party tracking before consent, contradicting the durable security/privacy contract and making CSP consent-session claims untestable. | Add a consent source of truth and banner, defer frontend tracker initialization until consent is accepted, keep no-op behavior when env vars are unset, and add tests/browser evidence for cold-load no-tracker and accept-loads-tracker behavior. | [#20](https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/20) |
| ADR-004 | low | confirmed | Planning hygiene; Integrate UI-02 deferral | The Integrate widget and README intentionally keep plugin download buttons `aria-disabled` as UI-02 future work (`app/src/components/viewer-widgets/Integrate/Integrate.tsx:47-63`, `app/src/components/viewer-widgets/Integrate/README.md:52-64`). The widget-access matrix says the same disabled-future download pipeline is UI-02 (`docs/agents/widget-access-matrix.md:41`), while durable `ui-views` still names plugin downloads in the F7 requirement (`openspec/specs/ui-views/spec.md:12-31`). GitHub `#4` was closed after proving the real F7 surface/reachability, but searches for `UI-02`, `agent integration download`, and `plugin downloads` found no exact open issue for the remaining download pipeline. | Future agents can read the source/README phrase "tracked separately under UI-02" and assume the plugin-download work has a backlog owner when it does not. | Create a backlog issue that either implements the plugin download manifest/pipeline with tests and browser replay, or deliberately updates the durable spec/source docs to treat disabled downloads as the accepted final affordance. | [#21](https://github.com/GroundX-Studio/workspace-groundx-v2-ui-a7a4fac7/issues/21) |
