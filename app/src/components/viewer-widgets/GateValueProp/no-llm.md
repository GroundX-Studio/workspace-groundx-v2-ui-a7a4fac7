# GateValueProp — no LLM tools

## Why

GateValueProp is a legacy, purely presentational value-prop pitch. It has no
interactive controls and no state the LLM could meaningfully drive. The live
sign-in path now uses `SignUpWidget`; adding a tool here would be inventing
surface area that does not exist.
